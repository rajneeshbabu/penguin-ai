# Penguin AI — Production RAG + Agentic RAG + RLHF + Voice
# Install: pip install -r requirements.txt

# Groq API (Llama 3.3 inference — free tier)
groq>=0.9.0

# LangChain
langchain>=0.2.0
langchain-community>=0.2.0
langchain-groq>=0.1.0
langchain-huggingface>=0.0.3

# Vector store
faiss-cpu>=1.8.0

# Embeddings (free, local)
sentence-transformers>=3.0.0

# Cross-encoder reranker (Layer 4 — ms-marco-MiniLM-L-6-v2)
# Already included in sentence-transformers

# BM25 sparse retrieval (Layer 3 hybrid)
rank-bm25>=0.2.2

# Document loaders
pypdf>=4.0.0
docx2txt>=0.8
pdfplumber>=0.11.0

# Streamlit UI
streamlit>=1.35.0

# Voice-to-text mic recorder (records audio → Whisper via Groq)
# Provides mic button in chat; gracefully disabled if not installed
streamlit-mic-recorder>=0.0.4

# Utilities
python-dotenv>=1.0.0
tiktoken>=0.7.0

# Notebook
jupyter>=1.0.0
ipykernel>=6.0.0
